/*
import WebRoutes from "./WebRoutes";
import { useAuth } from "../hooksAuth/useAuth";

export default function Routes() {
  const { isLoggedIn } = useAuth();
  if (isLoggedIn === null) return null;
  return isLoggedIn ? <WebRoutes /> : <SignRoutes />;
}
*/
